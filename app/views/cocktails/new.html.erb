<div class="container">
  <%= form_for [@cocktail] do |c| %>
    <%= c.label :name %>
    <%= c.text_field :name  %>
    <%= c.submit  %>
  <% end %>

  <%= link_to "Back to cocktails", cocktails_path, class:"btn btn-dark" %>
</div>
